# Streamlit Demo Requirements
streamlit>=1.28.0
plotly>=5.15.0
shap>=0.42.0
psutil>=5.9.0

# Data manipulation (if not already installed)
pandas>=1.5.0
numpy>=1.21.0

# ML libraries (if not already installed)
scikit-learn>=1.3.0
xgboost>=1.7.0
joblib>=1.3.0

# Neo4j connector (if not already installed)
neo4j>=5.8.0
